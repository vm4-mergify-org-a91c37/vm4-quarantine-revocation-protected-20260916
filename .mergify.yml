pull_request_rules:
  - name: merge only controlled quarantine gate green PRs
    conditions:
      - label = vm4-qrev-merge
      - check-success = quarantine-gate
    actions:
      merge:
        method: squash
